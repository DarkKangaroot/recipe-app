// import IToken from "../Interfaces/Login";

const isUserLogin = () => {
    // const tokenString: string | null = localStorage.getItem("token")
    // const token: IToken = tokenString != null ? JSON.parse(tokenString) : {isLogged : false};
    // if(token.isLogged){
    //     return true;
    // }
    // return false
    return true;
}

export default isUserLogin;